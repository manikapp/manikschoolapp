# Supabase — from your project's Settings > API page
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Used to sign/verify QR tokens (lib/qr.ts) — any long random string, keep it secret
QR_SIGNING_SECRET=
